import styled from 'styled-components';

export default styled.li`
  white-space: nowrap;
  overflow-x: auto;
  overflow-y: hidden;
  display: inline;
  list-style-type: none;
  margin: -15px;
  padding: 2px;
`